# tpc-releases

Public release channel for **RxAI**. Holds installer assets (GitHub Releases) and the update
manifest `latest.json`, which the app's `UpdateChecker` reads to offer self-updates.

`latest.json` is rewritten automatically by the `release.yml` workflow in `wvitamins/rxai-ui` on
each version tag. Do not edit it by hand.
